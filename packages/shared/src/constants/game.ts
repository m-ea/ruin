/**
 * Core game constants shared between client and server.
 */

/** Server tick rate in Hz. Used starting Phase 1. */
export const TICK_RATE = 20;

/** Maximum number of players per world instance */
export const MAX_PARTY_SIZE = 8;

/** Tile size in pixels for rendering */
export const TILE_SIZE = 16;
